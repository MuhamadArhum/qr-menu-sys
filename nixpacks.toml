[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm install -g pnpm@11.0.9", "pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm --filter @abyte/api build"]

[start]
cmd = "node apps/api/dist/main"
